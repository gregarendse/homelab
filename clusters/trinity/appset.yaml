# GENERATED FILE - DO NOT EDIT
# Source: clusters/trinity/apps.yaml
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: trinity-apps
  namespace: argocd
  labels:
    homelab.gregarendse/cluster: "trinity"
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=zero"]
  generators:
    - list:
        elements:
          - name: actual
            namespace: actual
            type: helm
            helmKind: path
            releaseName: actual
            chartPath: server
            valueFiles: ../applications/actual/values.yaml
          - name: deluge
            namespace: deluge
            type: helm
            helmKind: path
            releaseName: deluge
            chartPath: server
            valueFiles: ../applications/deluge/values.yaml
          - name: echo
            namespace: echo
            type: helm
            helmKind: path
            releaseName: echo
            chartPath: server
            valueFiles: ../applications/echo/values.yaml
          - name: plex
            namespace: plex
            type: helm
            helmKind: path
            releaseName: plex
            chartPath: server
            valueFiles: ../applications/plex/values.yaml
          - name: prowlarr
            namespace: prowlarr
            type: helm
            helmKind: path
            releaseName: prowlarr
            chartPath: server
            valueFiles: ../applications/prowlarr/values.yaml
          - name: radarr
            namespace: radarr
            type: helm
            helmKind: path
            releaseName: radarr
            chartPath: server
            valueFiles: ../applications/radarr/values.yaml
          - name: sabnzbd
            namespace: sabnzbd
            type: helm
            helmKind: path
            releaseName: sabnzbd
            chartPath: server
            valueFiles: ../applications/sabnzbd/values.yaml
          - name: sonarr
            namespace: sonarr
            type: helm
            helmKind: path
            releaseName: sonarr
            chartPath: server
            valueFiles: ../applications/sonarr/values.yaml
          - name: tautulli
            namespace: tautulli
            type: helm
            helmKind: path
            releaseName: tautulli
            chartPath: server
            valueFiles: ../applications/tautulli/values.yaml
          - name: wireguard
            namespace: wireguard
            type: helm
            helmKind: path
            releaseName: wireguard
            chartPath: server
            valueFiles: ../applications/wireguard/values.yaml
          - name: cert-manager
            namespace: security
            type: helm
            helmKind: remote
            releaseName: cert-manager
            chart: cert-manager
            helmRepoURL: https://charts.jetstack.io
            helmTargetRevision: v1.18.2
          - name: ingress-nginx
            namespace: ingress-nginx
            type: helm
            helmKind: remote
            releaseName: ingress-nginx
            chart: ingress-nginx
            helmRepoURL: https://kubernetes.github.io/ingress-nginx
            helmTargetRevision: 4.13.2
  template:
    metadata:
      name: trinity-{{.name}}
      namespace: argocd
      labels:
        homelab.gregarendse/cluster: "trinity"
        homelab.gregarendse/app: "{{.name}}"
    spec:
      project: default
      destination:
        server: https://kubernetes.default.svc
        namespace: "{{.namespace}}"
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ApplyOutOfSyncOnly=true
      source:
        {{- if eq .type "rendered" }}
        repoURL: https://github.com/gregarendse/homelab.git
        targetRevision: master
        path: clusters/trinity/rendered/{{.name}}
        directory:
          recurse: true
        {{- else if eq .helmKind "path" }}
        repoURL: https://github.com/gregarendse/homelab.git
        targetRevision: master
        path: "{{.chartPath}}"
        helm:
          releaseName: "{{.releaseName}}"
          {{- if .valueFiles }}
          valueFiles:
            - "{{.valueFiles}}"
          {{- end }}
        {{- else if eq .helmKind "remote" }}
        repoURL: "{{.helmRepoURL}}"
        targetRevision: "{{.helmTargetRevision}}"
        chart: "{{.chart}}"
        helm:
          releaseName: "{{.releaseName}}"
        {{- end }}
