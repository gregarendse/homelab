# GENERATED FILE - DO NOT EDIT
# Source: clusters/trinity/apps.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: trinity-ingress-nginx
  namespace: argocd
  labels:
    homelab.gregarendse/cluster: "trinity"
    homelab.gregarendse/app: "ingress-nginx"
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: ingress-nginx
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
  # Multi-source: chart from the upstream Helm repo, values from this Git repo
  # (referenced via the $values ref).
  sources:
    - repoURL: https://kubernetes.github.io/ingress-nginx
      targetRevision: 4.13.2
      chart: ingress-nginx
      helm:
        releaseName: ingress-nginx
        valueFiles:
          - $values/applications/nginx/values.yaml
    - repoURL: https://github.com/gregarendse/homelab.git
      targetRevision: master
      ref: values
